import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'fullstack-test-one',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
